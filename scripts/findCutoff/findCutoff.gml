function findCutoff(target_x, target_y){
	//find a guy between me and my target
	var newTarget = -1
	var my_x = x
	var my_y = y
	var distanceToTarget = point_distance(my_x,my_y,target_x,target_y)
	
	with(obj_baller_onfield){
		
		
	}
	
}